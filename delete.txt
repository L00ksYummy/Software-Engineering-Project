                scan.nextLine();

                System.out.println("Would you like to delete a patient, procedure, or procedure history?");
                String deleteSection = scan.nextLine().trim().toLowerCase();

                if (deleteSection.equals("patient")) {
                    System.out.println("Enter the MRN of the patient you want to delete:");
                    String mrn = scan.nextLine().trim().toUpperCase();

                    System.out.println("Are you SURE you want to delete patient " + mrn + "? (y/n)");
                    String confirm = scan.nextLine().trim().toLowerCase();

                    if (confirm.startsWith("y")) {
                        deletePatient.delete(mrn);
                    } else {
                        System.out.println("Delete cancelled.");
                    }

                } else if (deleteSection.equals("procedure")) {
                    System.out.println("Enter the Procedure ID of the procedure you want to delete:");
                    String pid = scan.nextLine().trim().toUpperCase();

                    System.out.println("Are you SURE you want to delete procedure " + pid + "? (y/n)");
                    String confirm = scan.nextLine().trim().toLowerCase();

                    if (confirm.startsWith("y")) {
                        deleteProcedure.delete(pid);
                    } else {
                        System.out.println("Delete cancelled.");
                    }

                } else if (deleteSection.equals("procedure history")) {
                    System.out.println("Enter the MRN for the procedure history you want to delete:");
                    String mrn = scan.nextLine().trim().toUpperCase();

                    System.out.println("Enter the Procedure ID for the procedure history you want to delete:");
                    String pid = scan.nextLine().trim().toUpperCase();

                    System.out.println("Are you SURE you want to delete history for MRN "
                            + mrn + " and Procedure " + pid + "? (y/n)");
                    String confirm = scan.nextLine().trim().toLowerCase();

                    if (confirm.startsWith("y")) {
                        deleteProcedureHistory.delete(mrn, pid);
                    } else {
                        System.out.println("Delete cancelled.");
                    }

                } else {
                    System.out.println("Invalid choice. Please type 'patient', 'procedure', or 'procedure history'.");
                
